name: CI

on:
  push:
    branches: [ bazelproj ]
  pull_request:
    branches: [ bazelproj ]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: bazelbuild/setup-bazelisk@v1
      - run: bazel build //...
      - run: bazel test //...
